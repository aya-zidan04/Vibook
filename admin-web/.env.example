# Optional. Examples:
# - (omit) → use /api/v1 on the Vite dev server; vite.config.ts proxies to Spring on :8080
# - VITE_API_BASE_URL=http://localhost:8080  → /api/v1 is appended automatically
# - VITE_API_BASE_URL=http://localhost:8080/api/v1
