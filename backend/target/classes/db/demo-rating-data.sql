-- Optional manual seed for rating tests (MySQL). Adjust IDs to match your database.
-- Requires existing rows in users, business_events (and related profile / category data from your app).
--
-- 1) CONFIRMED booking so the user may rate:
-- INSERT INTO bookings (user_id, event_id, status, created_at, updated_at)
-- VALUES (1, 1, 'CONFIRMED', UTC_TIMESTAMP(), UTC_TIMESTAMP());
--
-- 2) Or COMPLETED:
-- INSERT INTO bookings (user_id, event_id, status, created_at, updated_at)
-- VALUES (1, 1, 'COMPLETED', UTC_TIMESTAMP(), UTC_TIMESTAMP());
--
-- Then call:
--   GET  /api/v1/events/1        (Bearer token for user 1)
--   POST /api/v1/events/1/rate   body: {"rating": 4}
