# Copy to `.env` and set YOUR_MACHINE_IP (not committed). Expo injects `EXPO_PUBLIC_*` at bundle time.
# Physical device: use LAN IP, e.g. http://192.168.1.5:8080/api/v1
# Simulator on same machine: http://localhost:8080/api/v1 is fine
EXPO_PUBLIC_API_URL=http://YOUR_MACHINE_IP:8080/api/v1
