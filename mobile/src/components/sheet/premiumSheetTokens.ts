/** Shared layout tokens for premium modal sheets (Settings, Vouchers, etc.). */
export const premiumSheetTokens = {
  sheetTopRadius: 28,
  sheetMinHeightPct: '78%' as const,
  sheetMaxHeightPct: '88%' as const,
  cardRadius: 28,
  rowMinHeight: 82,
  rowPaddingH: 23,
  iconCircleSize: 52,
  iconCircleRadius: 16,
  buttonRadius: 9999,
  buttonMinHeight: 58,
  contentGap: 12,
} as const;
